Describe gina#router
  Describe #load_modules({category})
    It load modules in autoload/gina/{category}/*.vim
      let modules = gina#router#load_modules('command')
      Assert KeyExists(modules, 'commit')
      Assert KeyExists(modules, 'edit')
      Assert KeyExists(modules, 'status')
      Assert KeyExists(modules, 'show')
    End
  End
End
